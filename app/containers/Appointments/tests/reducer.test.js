
// import { fromJS } from 'immutable';
// import appointmentsReducer from '../reducer';

describe('appointmentsReducer', () => {
  it('returns the initial state', () => {
    // FIXME: add unit tests
    // expect(appointmentsReducer(undefined, {})).toEqual(fromJS({}));
  });
});
