// import { fromJS } from 'immutable';
// import userAccountsPageReducer from '../reducer';

describe('userAccountsPageReducer', () => {
  it('returns the initial state', () => {
    // expect(userAccountsPageReducer(undefined, {})).toEqual(fromJS({}));
  });
});
