// import { fromJS } from 'immutable';
// import { selectPermissionAssignmentsDomain } from '../selectors';

describe('selectPermissionAssignmentsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
