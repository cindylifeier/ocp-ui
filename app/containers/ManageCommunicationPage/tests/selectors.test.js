// import { fromJS } from 'immutable';
// import { selectManageCommunicationPageDomain } from '../selectors';

describe('selectManageCommunicationPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
