// import { fromJS } from 'immutable';
// import { selectManageRelatedPersonPageDomain } from '../selectors';

describe('selectManageRelatedPersonPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
