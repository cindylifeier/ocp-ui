// import { fromJS } from 'immutable';
// import { selectSelectConsentActorsDomain } from '../selectors';

describe('selectSelectConsentActorsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
