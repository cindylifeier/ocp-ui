// import { fromJS } from 'immutable';
// import { selectManagePractitionerPageDomain } from '../selectors';

describe('selectManagePractitionerPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
