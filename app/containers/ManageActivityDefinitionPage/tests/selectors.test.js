// import { fromJS } from 'immutable';
// import { selectManageActivityDefinitionPageDomain } from '../selectors';

describe('selectManageActivityDefinitionPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
