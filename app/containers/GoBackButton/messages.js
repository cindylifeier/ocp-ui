/*
 * GoBackButton Messages
 *
 * This contains all the text for the GoBackButton component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  label: {
    id: 'ocpui.containers.GoBackButton.label',
    defaultMessage: 'Cancel',
  },
});
