// import { fromJS } from 'immutable';
// import { selectCareCoordinatorUpcomingAppointmentDomain } from '../selectors';

describe('selectCareCoordinatorUpcomingAppointmentDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
