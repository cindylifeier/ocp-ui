// import { fromJS } from 'immutable';
// import { selectAssignLocationToPractitionerPageDomain } from '../selectors';

describe('selectAssignLocationToPractitionerPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
