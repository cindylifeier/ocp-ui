import { Grid } from 'styled-css-grid';

const SearchContainerStyledGrid = Grid.extend`
  padding: 5px 20px;
  margin: 2px 0;
`;

SearchContainerStyledGrid.propTypes = {};

export default SearchContainerStyledGrid;
