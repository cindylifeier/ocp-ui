import { Grid } from 'styled-css-grid';

const DetailsPanelGrid = Grid.extend`
  background-color: rgba(240, 248, 250, 1);
  min-height: 37px;
`;

DetailsPanelGrid.propTypes = {};

export default DetailsPanelGrid;
